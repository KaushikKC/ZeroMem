{
  "name": "@zeromem/openclaw-gateway",
  "version": "0.1.0",
  "description": "ZeroMem OpenClaw gateway plugin — auto-recall, auto-capture, memory tools, CLI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "zeromem": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest"
  },
  "peerDependencies": {
    "@zeromem/sdk": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
